[tool.poetry]
name = "forge"
version = "0.0.0-NOVERSION"
description = "Forge: Email App (DO NOT MODIFY. THIS FILE IS FOR DEPENDENCIES AND DEV-DEPENDENCIES.)"
authors = ["Goutham Krishna K V <gauthamkrishna9991@live.com>"]
license = "UNLICENSED"

[tool.poetry.dependencies]
python = "^3.10"
IMAPClient = "^2.3.1"

[tool.poetry.dev-dependencies]
pytest = "^7.2.0"
black = {version = "^22.12.0", allow-prereleases = true}
PyGObject = "^3.42.2"
req2flatpak = "^0.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
